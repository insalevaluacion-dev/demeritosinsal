# Metapaquete: la app Next.js vive en demeritos/
# En Railway → Settings → Root Directory = demeritos

[build]
builder = "NIXPACKS"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
